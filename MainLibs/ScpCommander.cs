using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Renci.SshNet;

namespace TejiAdesa.MainLibs
{
    public class ScpCommander 
    {

        /*public ScpCommander()
        {
            ConnectionInfo connectionInfo = new PasswordConnectionInfo(Server, User, Password);
            ScpClient scpClient = new ScpClient(connectionInfo);
        }

        private sealed class Authentication : AuthenticationMethod
        {

            public override AuthenticationResult Authenticate(Session session)
            {
                throw new NotImplementedException();
            }

            public override string Name
            {
                get { throw new NotImplementedException(); }
            }
        }*/
    }
}
